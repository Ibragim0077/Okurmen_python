def func_1(func):
    def wrapper():
        print("***** что - то делаем перед вызова функци")
 import time
file_1.py U X
def get_time( func):
    def wrapper(*args， **kwargs)：
        start_time = time.time()
        result = func(*args, **kwargs)
        end_time = time. time ( )
        print (result)
        rint(f"Time: (end_time - start_time)")
return wrapper

def get_gcd(a, b):
    while (a != b and a>0 and b >0):
        a, b = b, a% b
    return a + b


else:

b= b-a
return a
2 main*
def get_fast_gcd(a, b):
    while (a != band a > 0 and b > 0):
a, b = b, a & b
return a + b
get_gcd = get_time(func=get_gcd)
get_fast_gcd = get_time(func=get_fast_gcd)
get_gcd (10, 15)
get_fast_gcd (10, 15)       